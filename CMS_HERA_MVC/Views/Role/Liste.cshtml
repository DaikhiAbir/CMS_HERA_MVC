@model IEnumerable<CMS_HERA_MVC.Models.Role>

@{
    ViewBag.Title = "Liste";
}

<h2>Liste</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.titre_role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.permissions)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.position_role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.statut_role)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.id_role)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.titre_role)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.permissions)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.position_role)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.statut_role)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
